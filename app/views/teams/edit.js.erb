document.getElementById('team-<%= @team.id %>').innerHTML = '<%= j(render @team) %>'
var form = document.getElementsByTagName('form')[0]
form.outerHTML = '<%= j(render 'form', team: @team, value: 'チーム名編集') %>'
